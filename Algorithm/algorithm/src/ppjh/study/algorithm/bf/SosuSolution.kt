package com.kotlin.example.algo

class SosuSolution {
    fun solution(numbers: String): Int {
        var answer = 0

        var idx = 0
        var count = 0

        while(idx < numbers.length) {
//            val     
        }

        return answer
    }
}